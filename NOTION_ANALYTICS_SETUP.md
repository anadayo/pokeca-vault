# POKÉCA VAULT 日次レポート

毎朝9:15（JST）に前日のGA4と価格更新状況をNotionへ保存します。同じ日付を再実行した場合は新規行を増やさず、その行を更新します。

## Notionデータベース

以下のプロパティ名・種類でデータベースを作成します。

| プロパティ | 種類 |
| --- | --- |
| 日次レポート | タイトル |
| 日付 | 日付 |
| 月 | セレクト |
| アクティブユーザー | 数値 |
| 新規ユーザー | 数値 |
| セッション | 数値 |
| PV | 数値 |
| サイト計測クリック | 数値 |
| メルカリ公式クリック | 数値（手入力） |
| クリック差分 | 数式 `prop("サイト計測クリック") - prop("メルカリ公式クリック")` |
| CTR | 数値（表示形式をパーセント） |
| カード詳細表示 | 数値 |
| 検索回数 | 数値 |
| Xシェア | 数値 |
| 価格データ日 | 日付 |
| 価格更新 | セレクト |
| サイト | URL |
| 同期日時 | 日付 |

月次確認用に「月次」ビューを追加し、`月`でグループ化します。PV・ユーザー・クリックは合計、CTRは平均を表示すると月ごとの比較ができます。

## GitHub Secrets

リポジトリの Settings > Secrets and variables > Actions に以下を登録します。

- `NOTION_TOKEN`: Notionインテグレーションのトークン
- `GA_SERVICE_ACCOUNT_JSON`: Google Cloudサービスアカウント鍵のJSON全文

日次レポートのデータソースID `3d7c9239-b3c9-802c-8506-000b712815e5` はワークフローへ設定済みです。

Notionでは対象データベースの「接続」からインテグレーションを追加します。Google CloudではAnalytics Data APIを有効にし、サービスアカウントのメールアドレスをGA4プロパティ `552987217` の閲覧者として追加します。

## 手動実行

GitHub Actionsの「Save daily analytics to Notion」から実行できます。`report_date`を指定すると過去日の再取得もできます。

メルカリアンバサダー管理画面の公式クリック数は公開APIがないため自動取得せず、`メルカリ公式クリック`へ手入力します。サイト側のクリック数との差は`クリック差分`で確認できます。
